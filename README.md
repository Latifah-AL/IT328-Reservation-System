# IT328-Reservation-System
A Java-based online restaurant reservation system using sockets, multithreading, and GUI. Users can register, make, confirm, update, or cancel reservations while the system handles real-time table availability and concurrent client interactions efficiently.
